class HomeModel {
  String? time, priority, notes, date, title, key;
  int? checkupdate;

  HomeModel(
      {this.time,
      this.priority,
      this.notes,
      this.date,
      this.title,
      this.key,
      this.checkupdate});
}
