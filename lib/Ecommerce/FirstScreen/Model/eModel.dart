class eModel {
  String? name, image, desc, key, rating, price, date, time;
  int? checkUpdate;

  eModel(
      {this.name,
      this.image,
      this.desc,
      this.key,
      this.price,
      this.rating,
      this.date,
      this.time,
      this.checkUpdate});
}
